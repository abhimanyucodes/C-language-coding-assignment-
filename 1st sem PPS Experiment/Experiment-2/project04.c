//4. wap to swap values of two variables with or without another variable 
#include<stdio.h>
void main()
{
    int a,b,temp;
    printf("Enter two no a and b: ");
    scanf("%d %d", &a ,&b);
    // a=a+b;
    // b=a-b;       //without using third variable
    // a=a-b;


    // temp=a;
    // a=b;         //using third variable
    // b=temp;

    printf("After swap value of a and b : %d %d ",a,b);

    
}  